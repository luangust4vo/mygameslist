runtime: python312 
entrypoint: gunicorn -b :$PORT --workers 1 --threads 4 core.wsgi

handlers:
- url: /static
  static_dir: static_gcloud/
- url: /.*
  script: auto

automatic_scaling:
  min_instances: 0
  max_instances: 2

env_variables:
  DEBUG: "False"
  ALLOWED_HOSTS: "mygameslist-497823.rj.r.appspot.com"
  DJANGO_SETTINGS_MODULE: "core.settings"